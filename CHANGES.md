# Change History

**0.0.6** : fix Python 3.13 environment lookup for `PYDM_DISPLAYS_PATH`
    - replace invalid `pathlib.os` access with the standard `os` module
    - fix stylesheet loading when `PYDM_DISPLAYS_PATH` is set

**1.0.0** : initial release
    -tba-
